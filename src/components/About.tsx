import { app, Component } from 'apprun';

export default class About extends Component {
  state = 'About';

  view = state => {
    return (
      <div>
        ...
      </div>
    )
  }

  update = {

  };
}

